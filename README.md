# AlphaNiveshQuant (ANQuant)

ANQuant is a simplified broker-agnostic algorithmic trading platform built in Python. It supports fetching historical and real-time data, placing orders, and fetching portfolio and order details.

## Setup

1. Create a virtual environment and install dependencies:
   ```bash
   python -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt